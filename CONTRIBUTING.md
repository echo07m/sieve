# 贡献指南（CONTRIBUTING）

感谢你愿意为「剧合规」贡献力量！本项目尤其欢迎**规则库贡献**（新规则、平台审核口径更新、政策出处勘误），也欢迎代码、文档与测试改进。

## 行为准则

- 相互尊重，就事论事；对事不对人，欢迎不同观点的技术讨论。
- 禁止人身攻击、歧视性言论与任何形式的骚扰。
- 讨论合规口径时以政策原文与公开事实为依据，不传播未经证实的"内部消息"。

## 如何提 Issue

- **Bug 反馈**：请包含复现步骤、期望行为、实际行为、环境信息（Node/MySQL 版本、操作系统），涉及检测误判请附上**可公开分享的最小样例剧本片段**。
- **功能建议**：说明使用场景与价值，尽量给出期望的交互或 API 形态。
- **规则问题**（误判/漏判/出处勘误）：注明规则编号（ruleCode）、命中片段、你认为正确的口径与政策出处链接。
- 提交前请先搜索已有 Issue，避免重复。

## 如何提 PR

1. Fork 仓库并创建特性分支（见下文分支规范）。
2. 完成修改并自检：通过 PR 检查清单。
3. 提交 PR，填写改动说明；规则类 PR 必须附政策出处。
4. 响应评审意见，保持讨论聚焦在 PR 本身。

## 分支与提交信息规范

分支命名与提交信息统一使用前缀：

| 前缀 | 用途 | 示例 |
| --- | --- | --- |
| `feat/` | 新功能 | `feat/filing-material-export` |
| `fix/` | 缺陷修复 | `fix/cooccurrence-window-boundary` |
| `docs/` | 文档 | `docs/api-error-codes` |
| `rules/` | 规则库变更 | `rules/2026-09-platform-overrides` |

提交信息格式：`<前缀>: <一句话描述>`，例如 `rules: 新增红果平台擦边加严口径 R2-2026-003`。

## 规则贡献专项说明

规则库是本项目的核心资产，贡献前**必读** [docs/规则库扩展指南.md](docs/规则库扩展指南.md)。要点：

- 每条规则必须有明确的**政策出处**（sourcePolicy / sourceClause / originalText）。
- `sourceConfidence` 标注纪律：政策原文直引用 `official_text`；企服/行业解读口径必须标 `vendor_interpretation`，并在文案中注明"以广电总局原文为准"。
- 规则通过修改 `api/engine/seedData.ts` 增量入库，**不要**修改引擎代码来加规则。
- PR 中请附构造的样例剧本片段及预期命中结果（见扩展指南「测试方法」一节）。

## 代码风格

- 提交前运行 `npm run format`（Prettier）统一格式。
- `npm run check`（tsc）必须**零错误**；`npm run lint` 不应引入新告警。
- 检测引擎（`api/engine/`）保持**零外部运行时依赖**，便于独立移植与审计。
- 不提交任何真实密钥、`.env` 文件或用户数据。

## PR 检查清单

- [ ] 改动聚焦单一目的，无无关文件混入
- [ ] `npm run check` 零错误，`npm run lint` 无新增告警
- [ ] 已运行 `npm run format`
- [ ] 行为变更附带或更新了测试（`npm test` 通过）
- [ ] 规则类变更：政策出处齐全、sourceConfidence 标注正确、附样例验证
- [ ] 未包含任何密钥、.env 内容或隐私数据
- [ ] 文档（README/docs）随行为变更同步更新
